[build]
  base = "client"
  command = "npm run build"
  publish = "dist"

# SPA fallback — BrowserRouter routy (/login, /profily, …) musia vracať index.html
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
